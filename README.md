# watchfiles wheels for armv7 musleabihf

## Background

There is no prebuilt `watchfiles` wheels for armv7 musleabihf. This is an attempt to create those wheels.

## Usage

Grab the resulting wheel from the release and install using `pip` or `uv`.

## Thanks

Thanks Github for the action compute time and `samuelcolvin` for `watchfiles` package.

